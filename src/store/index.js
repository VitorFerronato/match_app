import { createStore } from 'vuex'
import snackbar from "./snackbar-store.js"
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    snackbar
  }
})
